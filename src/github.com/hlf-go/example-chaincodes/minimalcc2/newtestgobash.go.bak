package main

import (
	"fmt"
	"log"
	"os/exec"
)

func f() {
	out, err := exec.Command("/opt/gopath/src/github.com/hyperledger/fabric/examples/chaincode/go/minimalcc2/testPQC.sh").Output()
	if err != nil {
		log.Fatal(err)
	}
	fmt.Printf("output is %s\n", out)

}
